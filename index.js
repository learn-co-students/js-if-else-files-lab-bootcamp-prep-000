var name = "Susan"
var height = 70

// Don't worry about this
module.exports = { name, height
}

if(name === "Susan")
  { console.log("That is the correct name")
  else("That is the incorrect name")
}