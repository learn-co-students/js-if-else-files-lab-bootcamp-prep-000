var name = "Susan"
var height = "74"
var message = `${name} is ${height} inch tall`

if (name === "Susan") {
  console.log("The name is correct")
} else {
  console.log("Expected the name th be Susan")
}

// Don't worry about this
module.exports = { name, height, message
}
